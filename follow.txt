Reviewed the implementation plan for Phases 0-4. Approved with the following
changes before you proceed:

1. ACCESSIBILITY IS MISSING — add it back explicitly, not folded into "polish."
   In Phase 4, add:
   - Keyboard operability for every interactive control across all three
     modules (node add/remove, sliders, algorithm tabs, kill-leader,
     partition drawing — partition drawing specifically needs a keyboard-
     accessible alternative to click-drag, e.g. select two nodes via
     keyboard then a "partition" action).
   - aria-label / aria-live region per canvas summarizing current state in
     text (e.g. "8 nodes, 200 virtual nodes, ring rebalancing" / "Leader:
     Node 3, Term 4, 5 nodes healthy" / "3 nodes admitted, 1 dropped in last
     second"), updated on state change, not per animation frame.
   List this as its own file/component, e.g.
   src/components/modules/*/CanvasA11ySummary.tsx or equivalent per module.

2. PRODUCTION-LIMITATIONS CALLOUT — currently only scoped to
   ConsistentHashing/UnderTheHood.tsx. Extend the same [MODIFY] to
   RateLimiter/UnderTheHood.tsx and RaftConsensus/UnderTheHood.tsx as well,
   each with its own real citation (rate limiter memory/distributed-counter
   tradeoffs at scale; Raft paper link for what Raft-lite omits).

3. RAFT-LITE LABEL — add a small always-visible badge/banner component
   (not buried inside the collapsible Under the Hood panel) on the Raft
   module itself, e.g. src/components/modules/RaftConsensus/ScopeBadge.tsx,
   stating "Raft-lite: leader election + quorum only" with a link to the
   full Raft paper. This needs to be visible without the user opening
   anything.

4. URL STATE SYNC — src/lib/utils/url.ts needs explicit wiring, not just to
   exist in isolation. Each of hashRingStore.ts, rateLimiterStore.ts, and
   raftStore.ts should read initial state from URL search params on mount
   and write back on relevant state changes. Add this wiring as an explicit
   [MODIFY] on all three store files in Phase 4, referencing url.ts.

5. FPS BENCHMARKING — add an explicit dev-mode component,
   src/components/dev/FpsMonitor.tsx, using performance.now() deltas,
   toggleable, shown during the Phase 1/2/3 verification steps — not just
   listed as a manual verification bullet with no implementation.

6. CANVAS RESIZE — add a shared src/lib/hooks/useCanvasResize.ts (ResizeObserver-based)
   used by RingCanvas, PipelineCanvas, and ClusterCanvas, rather than handling
   resize ad hoc per component or leaving it to "check on narrow screens" in
   manual verification only.

7. CONFIRM: for the tab lazy-loading in Phase 0 (next/dynamic), confirm
   you're using { ssr: false } on any canvas-touching component import,
   since these touch window/document and the app uses static export
   (output: 'export') — this will fail the build otherwise if missed.

On your open questions:
- Test runner: plain Vitest is correct, no jsdom/@testing-library needed —
  everything under test is pure lib/algorithms/*.ts with no DOM interaction.
- Design language: see attached direction. Summary: dark charcoal base
  (#0E0F11 bg / #17181B surface), NOT pure black, warm off-white text
  (#E8E6E1). One functional accent color per module, not generic neon:
  amber (#E8A33D) for Consistent Hashing nodes/migrations, teal (#3DC9B0)
  admitted / coral (#E85D5D) denied for Rate Limiter (rest of that module
  stays grayscale so the signal isn't diluted), violet (#9B7EE8) leader /
  amber (#E8A33D) candidate / gray (#8B8D93) follower for Raft. Monospace
  (JetBrains Mono or IBM Plex Mono) for all Under the Hood code panels and
  numeric metrics. No glow/blur/bloom on accent colors. Fast, linear/ease-out
  animation timing (150-250ms), not slow floaty easing.

Proceed with Phase 0 once these are incorporated into the plan.